import { json,parseJson,safeText } from './lib/http.js';import { requireUser } from './lib/supabase-auth.js';
export default async function handler(req,res){const c=await requireUser(req);if(c.error)return json(res,{error:c.error},c.status);if(req.method==='GET'){const {data,error}=await c.admin.from('community_posts').select('id,user_id,display_name,body,created_at').order('created_at',{ascending:false}).limit(100);if(error)return json(res,{error:'Community needs database setup',setup:true},503);return json(res,{posts:data||[]});}
 if(req.method==='POST'){const b=await parseJson(req);const body=safeText(b.body,1000);if(!body)return json(res,{error:'Write a message first'},400);const display=(c.profile?.full_name||c.user.email?.split('@')[0]||'Member').slice(0,120);const {data,error}=await c.admin.from('community_posts').insert({user_id:c.user.id,display_name:display,body}).select('*').single();if(error)return json(res,{error:'Community needs database setup',setup:true},503);return json(res,{post:data});}
 if(req.method==='DELETE'){const b=await parseJson(req);const id=safeText(b.id,80);if(!id)return json(res,{error:'Missing post'},400);let q=c.admin.from('community_posts').delete().eq('id',id);if(c.profile?.role!=='admin')q=q.eq('user_id',c.user.id);const {error}=await q;if(error)return json(res,{error:'Could not remove post'},500);return json(res,{ok:true});}return json(res,{error:'Method not allowed'},405);}
