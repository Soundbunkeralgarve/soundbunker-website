{
  "name": "soundbunker-algarve-website",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check script.js && node --check api/availability.js && node --check api/create-checkout.js && node --check api/stripe-webhook.js"
  },
  "engines": { "node": ">=20" }
}
