# SoundBunker Algarve — Final Static Website

Target domain: www.soundbunker.pt (DNS currently managed separately).

This package contains the complete website front end, pages, imagery, video, pricing and booking/payment explanations. Google Calendar, Stripe and InvoiceXpress serverless integration files are present but credentials/configuration should be connected and tested individually after the front end is approved.

Vercel: import the folder/ZIP with `index.html` at the top level and Root Directory `./`.
