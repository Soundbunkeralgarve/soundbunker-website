(()=>{
const track=document.querySelector('#home-merch-track');if(!track)return;
const move=delta=>track.scrollBy({left:delta*Math.max(220,track.clientWidth*.75),behavior:matchMedia('(prefers-reduced-motion: reduce)').matches?'auto':'smooth'});
document.querySelector('#merch-prev').onclick=()=>move(-1);document.querySelector('#merch-next').onclick=()=>move(1);
const load=async()=>{try{const response=await fetch('/api/shop?action=products');if(!response.ok)throw Error('Unavailable');const data=await response.json();const clothing=data.products.filter(p=>/shirt|hoodie|sweatshirt|jacket|vest|shorts|joggers/i.test(p.name)&&p.image).slice(0,10);if(!clothing.length)return;track.replaceChildren();for(const p of clothing){const a=document.createElement('a');a.className='home-merch-card';a.href='shop.html#product-'+p.id;const img=document.createElement('img');img.src=p.image;if(p.campaign)img.className='campaign-image';img.alt=p.name;img.width=400;img.height=500;img.loading='lazy';img.decoding='async';const h=document.createElement('h3');h.textContent=p.name;a.append(img,h);if(Number.isSafeInteger(p.price)){const price=document.createElement('p');price.textContent='From '+new Intl.NumberFormat('en-IE',{style:'currency',currency:'EUR'}).format(p.price/100);a.append(price);}track.append(a);}}catch{/* Keep the visible shop link if the provider is unavailable. */}};
if('IntersectionObserver' in window){const observer=new IntersectionObserver(entries=>{if(entries.some(e=>e.isIntersecting)){observer.disconnect();load();}},{rootMargin:'300px'});observer.observe(track);}else load();
})();
